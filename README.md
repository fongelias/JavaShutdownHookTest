# JavaShutdownHookTest
A small test project for running shutdown hooks in a java application


## Environment
Vagrant
Spring Boot/Gradle
Java 8

## Startup
in root directory, run `vagrant up`
once the vm is running, run `vagrant ssh`
to access files, `cd /vagrant`
